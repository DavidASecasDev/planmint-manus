# Supabase Realtime Status

The `supabase_realtime` publication exists (ID: 16430) and lists ALL tables.
However, the test showed that events are NOT being received for `en_camino_tracking`.

This means the tables are listed but may not have their toggle enabled.
Need to find `en_camino_tracking` and `location_history` in the list and enable them.

The page shows all tables with toggle switches on the right side.
Need to search for "en_camino" in the search box and enable the toggle.
